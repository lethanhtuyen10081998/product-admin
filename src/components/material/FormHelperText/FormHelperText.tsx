import { FormHelperText as MaterialFormHelperText, FormHelperTextProps } from '@mui/material';
import React from 'react';

const FormHelperText = (props: FormHelperTextProps) => {
  return <MaterialFormHelperText {...props} />;
};

export default FormHelperText;
